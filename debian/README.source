This package is being improved and everything is tracked at :
https://bugs.debian.org/cgi-bin/bugreport.cgi?bug=772823

We don't veriify upstream source tarball as upstream don't sign releases.

override_dh_auto_test is used to skip tests as testing implies creating VMs
which is not always possible depending on the build environment/host.
